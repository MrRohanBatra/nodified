spring:
  datasource:
    url: ${MONITOR_DB_URL}
    username: ${MONITOR_DB_USERNAME}
    password: ${MONITOR_DB_PASSWORD}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${MONITOR_JWT_ISSUER_URI}

server:
  port: ${MONITOR_PORT:8082}

